rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /teachers/{teacherId} {
      allow read, write: if request.auth != null || teacherId == 'shared-school';
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
